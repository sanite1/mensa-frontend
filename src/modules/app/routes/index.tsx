// app.mensaproducts.com — scaffolded, NOT mounted at MVP.
// Reserved for a future separate logged-in product experience.

export function AppRoutes() {
  return (
    <div style={{
      display: 'flex', alignItems: 'center', justifyContent: 'center',
      minHeight: '100vh', fontFamily: 'var(--font-sans)', color: 'var(--mute)',
    }}>
      Coming soon.
    </div>
  )
}
